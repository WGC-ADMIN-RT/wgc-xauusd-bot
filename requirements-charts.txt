# DEPRECATED — the self-rendered mplfinance chart was removed. Charts are now rendered
# by Chart-IMG (TradingView) over HTTP; see charts.py. No local chart libs are needed.
# (File kept so any old deploy step referencing it is a harmless no-op.)
